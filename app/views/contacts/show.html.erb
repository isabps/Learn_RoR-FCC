<p style="color: green"><%= notice %></p>

<div class="jumbotron">
  <p class="display-4"><%= render @contact %></p>
  
  <%= link_to "Edit this contact", edit_contact_path(@contact), class:"btn btn-secondary" %> |
  <%= link_to "Back to contacts", contacts_path, class:"btn btn-dark" %>
</div>

<div>
  <br>
  <%= button_to "Destroy this contact", @contact, method: :delete, class:"btn btn-danger "%>
</div>
